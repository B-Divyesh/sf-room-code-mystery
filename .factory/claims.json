[
  {
    "id": "complete-case",
    "claim": "A case has three three-minute evidence rounds and reaches an answer screen.",
    "where": "landing page, game, README",
    "test": "npm test -- --grep @claim:complete-case",
    "sandbox": "fresh browser context at /demo; assert each round starts at 3:00, accuse the correct suspect, and assert the answer screen"
  },
  {
    "id": "room-code",
    "claim": "One room code synchronizes three rounds and the reveal across 4–8 player browsers while each notebook stays private.",
    "where": "landing page, game, README",
    "test": "npm test -- --grep @claim:room-code",
    "sandbox": "two fresh browser contexts; create and join an eight-player room, compare notebook one and eight, then assert host rounds and reveal reach the guest"
  },
  {
    "id": "demo-sandbox",
    "claim": "Demo sample data does not change real room data and can be reset.",
    "where": "demo banner, README",
    "test": "npm test -- --grep @claim:demo-sandbox",
    "sandbox": "fresh browser context with a real-namespace marker; change and reset demo state"
  },
  {
    "id": "local-privacy",
    "claim": "Demo room codes, notebook choices, timers, and accusations stay in the demo browser namespace.",
    "where": "demo banner, privacy page, README",
    "test": "npm test -- --grep @claim:local-privacy",
    "sandbox": "fresh browser context at /demo; submit an accusation, assert every game value is in demo storage, and assert the complete flow makes static GET requests only"
  },
  {
    "id": "offline-reload",
    "claim": "The demo reloads offline after the first visit.",
    "where": "landing facts, offline state, README",
    "test": "npm test -- --grep @claim:offline-reload",
    "sandbox": "dedicated browser context; load /demo, wait for its service worker, set offline, reload, and assert round one"
  },
  {
    "id": "additional-case",
    "claim": "Both authored cases are free, and paid cases are unavailable until product checkout exists.",
    "where": "room setup, additional-case section, terms, README",
    "test": "npm test -- --grep @claim:additional-case",
    "sandbox": "fresh browser context at /setup; choose The Orchid Ledger, create a room, and assert its first clue opens with no checkout link"
  },
  {
    "id": "settings-persist",
    "claim": "The sound choice persists on the device.",
    "where": "game settings, README",
    "test": "npm test -- --grep @claim:settings-persist",
    "sandbox": "fresh browser context at /demo; enable sound, reload, and assert it remains enabled"
  },
  {
    "id": "restart",
    "claim": "A finished case can restart at a new room lobby.",
    "where": "reveal screen",
    "test": "npm test -- --grep @claim:restart",
    "sandbox": "fresh browser context at /demo; finish the case, start a second case, and assert a reset lobby"
  },
  {
    "id": "render-rate",
    "claim": "The mobile game screen renders at 55 frames per second or better in the browser check.",
    "where": "handoff",
    "test": "npm test -- --grep @claim:render-rate",
    "sandbox": "390 by 844 Chromium viewport at /demo; sample 90 requestAnimationFrame intervals after initial paint"
  },
  {
    "id": "timer-focus",
    "claim": "Timer updates keep keyboard focus, and Space pauses the round.",
    "where": "game controls, README",
    "test": "npm test -- --grep @claim:timer-focus",
    "sandbox": "fresh browser context at /demo; focus Pause timer, wait for a timed decrement, press Space, and assert focus and time remain stable"
  },
  {
    "id": "focus-contrast",
    "claim": "The paper focus indicator has at least 3:1 contrast.",
    "where": "visual thesis, handoff",
    "test": "npm test -- --grep @claim:focus-contrast",
    "sandbox": "fresh browser context at /demo; read the focused timer button outline and calculate its contrast against both paper tokens"
  },
  {
    "id": "mobile-actions",
    "claim": "At 390 pixels wide, game actions are at least 44 pixels and the next-round action is above the fold.",
    "where": "visual thesis, README",
    "test": "npm test -- --grep @claim:mobile-actions",
    "sandbox": "390 by 844 Chromium viewport at /demo; measure every visible action and the next-round button position"
  },
  {
    "id": "not-found-status",
    "claim": "Unknown routes return HTTP 404 with a designed recovery page.",
    "where": "site configuration, handoff",
    "test": "npm test -- --grep @claim:not-found-status",
    "sandbox": "fresh browser context; request /missing-page, assert HTTP 404, and assert the recovery heading"
  },
  {
    "id": "cold-root-game",
    "claim": "The cold root opens an active sample game round, not room setup.",
    "where": "root route, README",
    "test": "npm test -- --grep @claim:cold-root-game",
    "sandbox": "fresh browser context at /; assert a round, timer, and next-round action, then advance the sample round"
  },
  {
    "id": "link-crawl",
    "claim": "Every site link reaches a working destination.",
    "where": "site header and footer, handoff",
    "test": "npm test -- --grep @claim:link-crawl",
    "sandbox": "fresh browser context crawls links from all product routes and requests each HTTP destination, including Hello Factory"
  },
  {
    "id": "route-metadata",
    "claim": "Every route has its own title, description, canonical URL, social title, focus restoration, and legal links.",
    "where": "all routes",
    "test": "npm test -- --grep @claim:route-metadata",
    "sandbox": "fresh browser context visits every route, checks metadata and footer links, then checks navigation and Back focus"
  },
  {
    "id": "reduced-motion",
    "claim": "Reduced-motion mode removes paper movement.",
    "where": "visual thesis, README",
    "test": "npm test -- --grep @claim:reduced-motion",
    "sandbox": "fresh browser context emulates reduced motion and measures the game sheet animation"
  },
  {
    "id": "no-account-media",
    "claim": "Playing needs no account, captures no voice or video, and uses no automated judgment.",
    "where": "landing privacy section, privacy page, README",
    "test": "npm test -- --grep @claim:no-account-media",
    "sandbox": "fresh browser context completes a real room while checking for account controls, media capture calls, AI, auth, and analytics requests"
  },
  {
    "id": "room-expiry",
    "claim": "Synchronized room state expires six hours after the last host action.",
    "where": "landing privacy section, privacy page, terms, README",
    "test": "npm test -- --grep @claim:room-expiry",
    "sandbox": "create a synchronized room and assert the server-assigned expiry is exactly six hours after its update timestamp"
  }
]
