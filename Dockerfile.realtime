FROM node:22-bookworm-slim
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci --omit=dev && npm cache clean --force
COPY server ./server
ENV PORT=8080 DATABASE_PATH=/data/rooms-v4.sqlite NODE_ENV=production
EXPOSE 8080
CMD ["node", "server/index.mjs"]
